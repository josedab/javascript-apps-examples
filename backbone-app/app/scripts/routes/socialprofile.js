/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var SocialprofileRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return SocialprofileRouter;
});
